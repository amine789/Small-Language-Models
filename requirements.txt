# ==============================================================
# Requirements for fine-tuning Small Language Models (SLMs)
# ==============================================================

# --- Core deep learning ---
torch>=2.2.0
torchvision
torchaudio

# --- Hugging Face ecosystem ---
transformers>=4.40.0
datasets>=2.19.0
tokenizers>=0.19.0
accelerate>=0.30.0
evaluate>=0.4.2
huggingface_hub>=0.23.0
safetensors>=0.4.3

# --- Parameter-efficient / low-resource fine-tuning ---
peft>=0.11.0          # LoRA, QLoRA, prefix-tuning, adapters
trl>=0.9.0            # SFTTrainer, DPO/PPO trainers for instruction/RLHF fine-tuning
bitsandbytes>=0.43.0  # 8-bit / 4-bit quantization
optimum>=1.20.0        # hardware-accelerated inference/export
auto-gptq>=0.7.1       # GPTQ post-training quantization
autoawq>=0.2.5         # AWQ quantization

# --- Distributed / large-scale training (optional but common) ---
deepspeed>=0.14.0
flash-attn>=2.5.8      # requires CUDA; skip on CPU/macOS
xformers

# --- Tokenization helpers ---
sentencepiece>=0.2.0
protobuf>=4.25.0
tiktoken>=0.7.0

# --- Data handling ---
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0
pyarrow>=15.0.0
scikit-learn>=1.4.0

# --- Text preprocessing / NLP utilities ---
nltk>=3.8.1
spacy>=3.7.4
regex>=2024.5.15
ftfy>=6.2.0

# --- Evaluation metrics ---
sacrebleu>=2.4.2
rouge-score>=0.1.2
bert-score>=0.3.13
seqeval>=1.2.2

# --- Experiment tracking / logging ---
wandb>=0.17.0
tensorboard>=2.16.2
mlflow>=2.13.0

# --- Visualization ---
matplotlib>=3.8.4

plotly>=5.22.0

# --- Notebook / interactive environment ---
jupyter>=1.0.0
ipywidgets>=8.1.2
tqdm>=4.66.4

# --- API-based / closed-source model access (optional) ---

anthropic>=0.28.0

# --- Serving / inference (optional, for deployment after fine-tuning) ---
vllm>=0.4.2
fastapi>=0.111.0
uvicorn>=0.29.0

# --- Config / experiment management ---
pyyaml>=6.0.1
omegaconf>=2.3.0
hydra-core>=1.3.2
python-dotenv>=1.0.1
